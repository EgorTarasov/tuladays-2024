meta {
  name: Get Dashboard for User
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/dashboard/patient/:id
  body: none
  auth: bearer
}

params:path {
  id: 42
}

auth:bearer {
  token: {{token}}
}
